<?php
require_once 'bootstrap.php';
require_once 'includes/common.inc';
require_once 'includes/stock-management-lib.inc';
$header = array ('Mark','Date','Item code','Quantity');
$list_user = new Savant3();
$list_user->title = 'List item';
$list_user->links = get_menu();
$list = list_stocks();
foreach ($list as $index => $item)  {
  $check_box = "<input name=\"codes[{$item['item_code']}]\" type='checkbox' value='1'/>";
  array_unshift($list[$index],$check_box);
}
$rows = $list;
$list_user->inputs = list_stock_form($header,$rows); 
$list_user->display('templates/content.tpl.php');
include 'footer.php';
?>
